import React from 'react';
import styled from 'styled-components';

const Header = styled.header`
  padding: 15px;
  position: relative;
  font-weight: normal;
  font-family: 'Open Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif;
  font-size: 32px;
  text-align: center;
  color: #333;
`;

export default () => <Header>Poker Cards</Header>;
